{
  "name": "vite-tauri",
  "version": "0.0.0",
  "scripts": {
    "dev": "parcel index.html --dist-dir dist --port 4000",
    "build": "tsc --noEmit && parcel build index.html --dist-dir dist --port 4000",
    "serve": "parcel serve index.html --dist-dir dist --port 4000"
  },
  "dependencies": {
    "@types/sharp": "^0.27.1",
    "concurrently": "^6.0.2",
    "tauri": "^0.14.1"
  },
  "devDependencies": {
    "@parcel/transformer-elm": "2.0.0-nightly.2273",
    "elm": "^0.19.1-5",
    "parcel": "^2.0.0-nightly.649"
  }
}
